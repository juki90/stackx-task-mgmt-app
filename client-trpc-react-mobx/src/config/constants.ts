const ROLES = {
    ADMIN: 'admin',
    USER: 'user'
};

const REACT_QUERY = {
    retry: false,
    refetchOnWindowFocus: false
};

const DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';

export { ROLES, REACT_QUERY, DATE_FORMAT };
