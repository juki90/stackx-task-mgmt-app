import { Box, Container } from '@mui/material';

import type { FC } from 'react';

export const Header: FC = () => (
    <Box bgcolor="primary.text">
        <Container>HEADER</Container>
    </Box>
);
