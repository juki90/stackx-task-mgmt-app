const ROLES = {
    ADMIN: 'admin',
    USER: 'user'
};

const REACT_QUERY = {
    staleTime: 5 * 60 * 1000,
    retry: false
};

const DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';

export { ROLES, REACT_QUERY, DATE_FORMAT };
